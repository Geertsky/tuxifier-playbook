---

- name: tuxifier
  hosts: installer-uefi,installer-bios
  tasks:
    - name: Import role tuxifier
      ansible.builtin.import_role:
        name: geertsky.tuxifier.ansible_tuxifier
      environment:
        PATH: "/local/conda/envs/tuxifier-python/sbin:/local/conda/envs/tuxifier-python/bin:{{ ansible_facts['env']['PATH'] | default('/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin') }}"
        CONDA_PREFIX: "/local/conda/envs/tuxifier-python"

    - name: "Wait for bootup to finish"
      ansible.builtin.wait_for_connection:
        delay: 5
        timeout: 600

    - name: Gather facts for first time
      ansible.builtin.setup:

    - name: Continue install
      when: continue_install is defined and continue_install
      ansible.builtin.include_tasks:
        file: continue_install.yml
      vars:
        ansible_python_interpreter: >-
          {{ '/usr/bin/python3'
            if ansible_facts['distribution_major_version'] | int == 8
            else 'auto_silent'}}
